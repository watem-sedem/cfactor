name: cfactor
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pre-commit
  - tox
  ## Python data science stack
  - numpy
  - pandas
  - geopandas
  - matplotlib  
  - pip:
     - -e .  # install git checkout of cfactor in editable mode
